const arrayA = [5, 32, 3, 44, 1];
const arrayB = [57, 4, 21, 2, 13];

let i = 0;
for (let num of arrayA) {
  const num2 = arrayB[i];
  let menorValor;

  if (numA < numB) {
    menorValor = num;
  } else {
    menorValor = num2;
  }

  console.log(menorValor);
  i++;
}

